<script>
  import initBaguetteBox from '../../utils/baguetteBox'
  import initLanguageChanger from '../../utils/langChanger'
  import initGsap from '../../utils/gsap'
  import initMixitup from '../../utils/mixitup'
  import Iconify from '@iconify/iconify'

  window.addEventListener('load', function() {
    initGsap()
    initMixitup()
    initBaguetteBox()
    initLanguageChanger()
  })
</script>
